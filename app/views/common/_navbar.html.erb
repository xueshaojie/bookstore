<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
      <a class="navbar-brand" href="/">书城</a>
    </div>
    <div class="collapse navbar-collapse" id="myNavbar">
      <ul class="nav navbar-nav">
        <li class="dropdown">
          <a class="dropdown-toggle" data-toggle="dropdown" href="#">分类浏览
            <span class="caret"></span>
          </a>
          <ul class="dropdown-menu">
              <% Category.all.each do |category| %>
                <li class="list-group-item"><a href="<%= category_path(category) %>"><%= category.name %></a></li>
              <% end -%>
          </ul>
        </li>
        <li>
          <%= link_to("产品", products_path) %>
        </li>
        <li>
          <%= link_to '关于我们', '#' %>
        </li>
        <li>
          <%= form_tag search_products_path, method: :get, class: "navbar-form navbar-left" do %>
            <div class="form-group">
              <% if @category %>
                <input type="hidden" name="category_id" value="<%= @category.id %>" />
              <% end %>
              <input type="text" name="w" value="<%= params[:w] %>" class="form-control search-input" placeholder="<%= @category ? "在 #{@category.name} 下搜索.." : '搜索整站商品..' %>">
            </div>
            <button type="submit" class="btn btn-default">搜索</button>
          <% end %>
        </li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
          <li><%= link_to "购物车(#{@shopping_cart_count})", shopping_carts_path %></li>
          <% if !current_user %>
            <li><a href="<%= new_user_registration_path %>">注册</a></li>
            <li><a href="<%= new_user_session_path %>">登录</a></li>
          <% else %>
            <li><a href="#">欢迎你, <%= current_user.username %></a></li>
            <li><%= link_to "退出", destroy_user_session_path, method: :delete %></li>
          <% end -%>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">个人中心 <span class="caret"></span></a>
            <ul class="dropdown-menu">
              <% if current_user && current_user.admin? %>
                  <li><a href="<%= admin_categories_path %>">后台管理</a></li>
              <% end %>
              <li><a href="<%= dashboard_orders_path %>">我的订单</a></li>
              <li><a href="<%= dashboard_addresses_path %>">我的收货地址</a></li>
              <li><a href="#">我的收藏</a></li>
              <li role="separator" class="divider"></li>
              <li><a href="#">个人信息</a></li>
            </ul>
          </li>
      </ul>
    </div>
  </div>
</nav>
